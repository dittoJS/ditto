import vElseDir from './v-else';
import vIfDir from './v-if';
import vForDir from './v-for';
import vBindDir from './v-bind';
import vOnDir from './v-on';

export default {
    'v-if': vIfDir,
    'v-else': vElseDir,
    'v-for': vForDir,
    'v-bind': vBindDir,
    'v-on': vOnDir
};